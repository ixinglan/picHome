{
  "name": "pichome-desktop",
  "version": "1.0.0",
  "private": true,
  "description": "PicHome 桌面端 (Tauri + Django sidecar)",
  "scripts": {
    "dev": "tauri dev",
    "build": "tauri build",
    "tauri": "tauri"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
