# 不要把密钥、本地数据、虚拟环境打进镜像
.env
.venv
__pycache__/
*.py[cod]
db.sqlite3
media/
static_collected/
.git
.gitignore

db_data
media_data

.idea/
.vscode/
.DS_Store
.workbuddy

# ===== 以下内容与 Web(Django) 镜像无关，必须排除，否则 COPY . . 会把它们打进镜像 =====
# desktop/ 是 Tauri 桌面端（含 target/ 2G+、node_modules/），Web 镜像完全不需要，
# 这正是镜像臃肿的根因。
desktop/
# 前端/IDE/构建缓存等本地产物
build/
dist/
.idea/
docs/
# skills/ 是给 AI Agent 用的技能目录（含 CLI 脚本），无需进运行镜像
skills/
node_modules/
# 根目录文档无需进运行镜像
*.md